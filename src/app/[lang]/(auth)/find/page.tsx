import React, { FC } from 'react';

import styles from './styles.module.scss';

interface ResetProps {}

const Reset: FC<ResetProps> = ({}) => {
  return <div className={styles.container}>Find Kurator</div>;
};

export default Reset;
